/**
 * Nexa — App Configuration
 * 
 * Set your Gemini API key here. This key is bundled into the app
 * and used transparently — users never need to see or enter it.
 * 
 * Get a free key at: https://ai.google.dev/
 * 1. Go to Google AI Studio → Get API Key
 * 2. Create a key and paste it below
 */

export const GEMINI_API_KEY = process.env.EXPO_PUBLIC_GEMINI_API_KEY || 'PASTE_YOUR_GEMINI_API_KEY_HERE';
